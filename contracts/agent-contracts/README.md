# Pinned agent-contracts snapshot

This directory is a generated offline snapshot of the schemas needed to validate
the orchestrator's canonical run records. Its source revision and the SHA-256 of
every copied byte are recorded in `PROVENANCE.json` and verified by the Rust
conformance test. Do not edit these schemas locally; refresh every file from the
recorded upstream revision and regenerate the provenance checksums together.
